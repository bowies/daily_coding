--Mysql
SELECT
    LEFT(PRODUCT_CODE, 2) AS CATEGORY,
    COUNT(PRODUCT_ID)
FROM
    PRODUCT
GROUP BY
    CATEGORY
ORDER BY
    CATEGORY

--Oracle
SELECT
    SUBSTR(PRODUCT_CODE, 0, 2) AS CATEGORY,
    COUNT(PRODUCT_ID)
FROM
    PRODUCT
GROUP BY
    SUBSTR(PRODUCT_CODE, 0, 2)
ORDER BY
    CATEGORY